class User {
  constructor(id, userId, fullName, surname, race) {
    this.id = id;
    this.userId = userId;
    this.fullName = fullName;
    this.surname = surname;
    this.race = race;
  }
}

export default User;
